---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: '8'

inputs:
- name: cloud-native-tour-repo
outputs:
- name: build 

run:
  path: cloud-native-tour-repo/ci/create-service.sh 
